var underscore = require("underscore");

var CK_generator = {
  //////////////////////////////////////////
  get_frugale_concepts : function(keyword,level,cb){
    var concepts = [];
    
    if(cb) cb(dd_per_keyword);
    else return dd_per_keyword;
  },
  //////////////////////////////////////////
}

module.exports = Object.create(CK_generator);


